# Project Instructions

## PR Reviews
When reviewing PR comments, use `gh api repos/cocodedk/7kasif/pulls/{number}/comments` and `gh pr view {number} --json reviews` to read CodeRabbit feedback directly.
